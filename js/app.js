angular.module("demoRest", [])
	.config(function() {
		/* Fase de configuración, aquí podemos trabajar con providers */
	})
	.run(function() {
	  /* Fase de ejecución, aquí podemos trabajar con recetas (que son instancias de providers) */
	});
